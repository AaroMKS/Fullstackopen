    sequenceDiagram

        participant browser
        participant server
        
        Note right of browser: User clicks "save"
    
        browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note
        activate server
        Note right of server: New note is saved on server
        server-->>browser: JSON response
        deactivate server

        Note right of browser: New note is rendered without reloading the page
